{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["lastUpdated", "source", "team", "metric", "unit", "values"],
  "properties": {
    "lastUpdated": { "type": "string", "format": "date-time" },
    "source": { "$ref": "#/definitions/source" },
    "team": { "type": "string" },
    "metric": { "type": "string" },
    "unit": { "type": "string" },
    "values": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["date", "value"],
        "properties": {
          "date": { "type": "string", "format": "date" },
          "value": { "type": "number" }
        }
      }
    },
    "summary": { "type": "string" }
  },
  "definitions": {
    "source": {
      "type": "object",
      "required": ["name", "url", "updateCadence"],
      "properties": {
        "name": { "type": "string" },
        "url": { "type": "string", "format": "uri" },
        "updateCadence": { "type": "string" }
      }
    }
  }
}
